# ──────────────────────────────────────────────
# MCP Trading Server — Python Dependencies
# ──────────────────────────────────────────────
# Install with:  pip install -r requirements.txt

# MCP Framework
mcp>=1.0.0

# Web / API Server  (cloud version only)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
starlette>=0.36.0

# Data & Querying
duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.26.0
boto3>=1.34.0          # S3/R2 client (cloud version only)

# Quant Analytics
quantstats>=0.0.62

# Charting
matplotlib>=3.8.0

# Bridge (local bridge only)
requests>=2.31.0

# Utilities  (already in stdlib but pinned for reproducibility)
# json, os, re, io, base64, threading, asyncio — no install needed